spring:
  social:
    twitter:
      appId: X
      appSecret: X
      auto-connection-views: true
  redis:
    host: localhost
    port: 6379
    password: null
    database: 0
    pool:
      max-idle: 8
      min-idle: 0
      max-active: 8
      max-wait: -1
security:
  basic:
    enabled: false
